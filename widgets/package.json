{
  "name": "scientific-calculator-widgets",
  "version": "1.0.0",
  "description": "Scientific Calculator MCP App widgets — keypad and step",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && npm run build:keypad && npm run build:step",
    "build:keypad": "WIDGET=keypad vite build",
    "build:step": "WIDGET=step vite build"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.1"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
